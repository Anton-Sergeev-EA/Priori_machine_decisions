# Реальные секреты — .env.example версионируется, .env — никогда.
.env
.env.local

# Артефакты локальной сборки (build.sh собирает через buildx --load,
# ничего не кладёт в рабочую директорию, но на всякий случай).
build/
*.tar

.DS_Store
.vscode/
.idea/
